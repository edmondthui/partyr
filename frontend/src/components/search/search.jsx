import React from 'react';
import PartyMap from './../map/party_map';
// in routes, app.js make sure rendering search container instead of party index / party map
//if you want to see map in this search pres should render other comps

const Search = ({ }) => (
    <div>
        <PartyMap/>
    </div>
)
export default Search;
