import { connect } from 'react-redux';
import Search from './search';

const mstp = state => {
    return {
        
    }
}

const mdtp = dispatch => {
    return {
        
    }
}

export default connect(mstp, mdtp)(Search);